import {ThreeBounce } from 'better-react-spinkit'
const Lodding = () => {
  return (
    <center style={{display:'grid', placeItems:'center', height:'100vh'}}>
      <ThreeBounce  size={30} color="#00a859"/>
    </center>
  );
};

export default Lodding;
